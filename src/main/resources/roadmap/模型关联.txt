模型创建模型
    模型字段可以是另外一个模型, 执行时创建另外一个模型
    一对一
        模型执行一次创建一个,
        模型迭代执行创建一个, 指定唯一id避免重复创建
    一对多
        模型迭代执行创建多个, 不指定唯一id, 查询不到则创建新的关联模型


模型引用模型
    一对一
        通过唯一id查询唯一记录
    一对多
        通过id查询关联记录, 比如明细表数据


